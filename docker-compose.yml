services:

  backend:
    build:
      context: ./backend
    container_name: currency_backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend/currency-app
    container_name: currency_frontend
    ports:
      - "4200:80"
    depends_on:
      - backend